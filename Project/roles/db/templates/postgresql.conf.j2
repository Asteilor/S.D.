listen_addresses = '*'
wal_level = replica
max_wal_senders = 3
max_replication_slots = 5  # Добавили (обязательно для слотов!)
wal_keep_size = 1GB
hot_standby = on   # Нужен на slave, но harmless на master тоже
data_directory = '/var/lib/postgresql/{{ pg_version }}/main'


